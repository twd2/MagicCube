#!/bin/bash

if [ -f Makefile ]; then
  echo 'Makefile already exists.'
  exit 0
fi

if uname | grep 'Darwin'; then
  echo 'cp Makefile.osx Makefile'
  cp Makefile.osx Makefile
else
  echo 'cp Makefile.linux Makefile'
  cp Makefile.linux Makefile
fi
